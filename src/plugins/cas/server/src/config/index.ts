export default {
  // default: ({ env }) => ({
  //   url: env('CAS_URL', 'https://example.com/cas'),
  // }),
  // validator: (config: { serverUrl: string; url: string }) => {
  //   if (config.serverUrl.trim() === '') {
  //     throw new Error('CAS_SERVER_URL is required');
  //   }
  //   if (!config?.url) {
  //     throw new Error(
  //       `The absolute URL of your backend must be defined.\
  //       See https://docs.strapi.io/cms/configurations/users-and-permissions-providers#setting-up-the-server-url`,
  //     );
  //   }
  // },
};
